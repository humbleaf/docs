{
  "_comment": "Single source of truth for the public API surface. Changes require explicit review. IC-13: agents must re-read this file before adding routes to any controller.",
  "denyPaths": [
    "/metrics",
    "/events/stream",
    "/api/v1/credits/topup",
    "/webhook/farcaster",
    "/webhook/x402/submit",
    "/api/v1/stripe/webhook",
    "/api/v1/stripe/checkout/subscription",
    "/api/v1/stripe/checkout/topup",
    "/api/v1/stripe/portal",
    "/api/v1/public/leaderboard",
    "/api/v1/auth/github",
    "/api/v1/auth/github/callback"
  ],
  "denyPathPrefixes": [
    "/admin",
    "/api/v1/fingerprints",
    "/api/v1/leaderboard"
  ],
  "denyServers": [
    "http://localhost:3000"
  ],
  "requireAuthAnnotation": {
    "/v1/intent": "bearer",
    "/v1/prior/claim": "bearer or x-api-key",
    "/api/v1/fingerprints": "bearer or x-api-key",
    "/api/v1/fingerprints/{agentId}": "bearer or x-api-key",
    "/api/v1/credits/balance/{operatorId}": "bearer",
    "/api/v1/operator/export": "bearer",
    "/api/v1/operator/guardrails": "bearer",
    "/api/v1/operator/kill-switch": "bearer"
  },
  "expectedPublicRouteCount": 53,
  "_expectedPublicRouteCount_note": "IC-19 rename guard. Update intentionally when adding or removing public routes."
}
